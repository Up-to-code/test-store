<div>
    {% for item in component.whatsapp %}
    <a class="fixed bottom-5 right-5 bg-teal-800 text-white p-3   flex justify-center items-center z-40 rounded-full" href="https://wa.me/{{ whatsapp.number }}?text={{ message }}" target="_blank">
        <i class="sicon-whatsapp text-2xl"></i>
    </a>
  
    {% endfor %}
</div>